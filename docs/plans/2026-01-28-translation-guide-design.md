# Translation Guide Sheet 設計文檔

## 背景

Apple Translation 框架的 `.translationTask` modifier 會在語言包未下載時**強制**顯示系統下載提示，這是 Apple 的設計限制，無法繞過。

這會造成用戶困惑：
- 用戶可能是臨時下載 App 想馬上使用
- 突然跳出的系統彈窗讓用戶不知道該怎麼選擇

## 解決方案

在系統彈窗出現前，顯示 App 自己的引導提示，讓用戶了解接下來會發生什麼。

## 流程

```
用戶按下翻譯
    ↓
檢查語言包狀態（LanguageAvailability API）
    ↓
已下載 → 直接翻譯
未下載 + 未看過引導 → 顯示 TranslationGuideSheet
    ↓
用戶點「繼續翻譯」
    ↓
如果勾選「不再顯示」→ 記住設定（@AppStorage）
    ↓
系統彈窗出現（用戶已有預期）
```

## UI 設計

```
┌─────────────────────────────────┐
│                                 │
│      🌐                         │
│                                 │
│   首次翻譯設定                    │
│                                 │
│   系統將詢問是否下載離線語言包。    │
│   你可以：                        │
│                                 │
│   • 點「下載」→ 之後可離線翻譯     │
│   • 點「完成」→ 暫時使用網路翻譯   │
│                                 │
│   ┌─────────────────────────┐   │
│   │        繼續翻譯          │   │
│   └─────────────────────────┘   │
│                                 │
│   ☑ 不再顯示                     │
│                                 │
└─────────────────────────────────┘
```

## 實作檔案

| 檔案 | 用途 |
|------|------|
| `DesignSystem/Components/TranslationGuideSheet.swift` | 引導提示 UI 元件 |
| `Features/Translation/TranslationView.swift` | 整合邏輯 |
| `Resources/Localizable.xcstrings` | 多語言本地化 |

## 技術細節

### 語言包狀態檢查

```swift
private func checkLanguageAvailability(
    source: Locale.Language,
    target: Locale.Language
) async -> Bool {
    let availability = LanguageAvailability()
    let status = await availability.status(from: source, to: target)
    return status == .installed
}
```

### 狀態管理

```swift
@State private var showTranslationGuide = false
@AppStorage("hasSeenTranslationGuide") private var hasSeenGuide = false
@State private var dontShowGuideAgain = false
```

## 優點

- **不阻擋進入 App** — 用戶可以先看看介面
- **精準觸發** — 只在需要時才提示
- **降低困惑** — 用戶知道接下來會發生什麼
- **可關閉** — 勾選「不再顯示」後不會再打擾

## 日期

2026-01-28
